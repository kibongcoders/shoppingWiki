#DB
spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/shoping_wiki?enabledTLSProtocols=TLSv1.2&characterEncoding=utf-8&autoReconnect=TRUE&rewriteBatchedStatements=true&serverTimezone=Asia/Seoul
#    username: root
#    password: 3025
#    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        format_sql: true
        order_updates: true
        order_inserts: true
        default_batch_fetch_size: 100
        jdbc:
          batch_size: 500
  data:
    web:
      pageable:
        default-page-size: 100
  thymeleaf:
    cache: true
  application:
    name: shoppingwiki
  config:
    import: optional:configserver:http://127.0.0.1:8888
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: admin
    password: 1234
  main:
    lazy-initialization: true

#logging
logging:
  exception-conversion-word: '%wEx'
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} %clr(${LOG_LEVEL_PATTERN:-%5p}){green} %clr([%22thread]){magenta} %clr(%-40.40logger{39}){cyan} %clr(: %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}){faint}'
  level:
    org:
      hibernate:
        sql: debug
        type: trace
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG


server:
  port: 0

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

management:
  endpoints:
    web:
      exposure:
        include: refresh , health , beans , busrefresh

#token:
#  expiration_time: 86400000
#  secret: 97F296E4586CDA219CF5A1F463B97394EAEFCDE349A6FF67E496C9EB5F
springdoc:
  swagger-ui:
    enabled: true
    disable-swagger-default-url: true
    display-request-duration: true
    operations-sorter: alpha
  api-docs:
    enabled: true
    path: /shoppingwiki/v3/api-docs
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8





